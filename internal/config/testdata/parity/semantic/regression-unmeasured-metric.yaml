# expect: regression.cpu_total is set but this benchmark does not measure cpu
version: "1"
name: x
benchmarks:
  - name: a
    commands: {a: {command: [a]}}
    regression: {cpu_total: {max_percent: 5}}
