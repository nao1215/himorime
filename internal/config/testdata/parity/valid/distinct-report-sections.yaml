version: "1"
name: outputs
benchmarks:
  - name: test
    commands: {app: {command: [app]}}
report:
  outputs:
    - {format: markdown, path: report.md, section: first}
    - {format: markdown, path: report.md, section: second}
