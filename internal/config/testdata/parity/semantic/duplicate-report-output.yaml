# expect: conflicts with report.outputs[0]
version: "1"
name: outputs
benchmarks:
  - name: test
    commands: {app: {command: [app]}}
report:
  outputs:
    - {format: json, path: out.json}
    - {format: csv, path: ./out.json}
