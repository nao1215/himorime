# expect: ${workdir} is not available in report.versions
version: "1"
suite: {name: x}
benchmarks:
  - name: a
    commands: {a: {command: [a]}}
report:
  versions:
    a: ["${workdir}/a", --version]
