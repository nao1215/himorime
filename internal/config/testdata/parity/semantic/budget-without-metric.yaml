# expect: a budget on peak rss needs the benchmark to measure memory
version: "1"
suite: {name: x}
benchmarks:
  - name: a
    commands: {a: {command: [a]}}
    budget: {a: {memory: {peak_rss: {max: "<= 64MiB"}}}}
