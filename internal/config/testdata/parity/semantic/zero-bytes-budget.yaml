# expect: the limit must be greater than zero
version: "1"
name: x
benchmarks:
  - name: a
    metrics: {memory: true}
    commands:
      a:
        command: [a]
        budget: {peak_rss: {max: "< 0KiB"}}
